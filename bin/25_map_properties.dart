void main()
{
  var person = new Map();
  person['name'] = 'Mahid';
  person['age'] = 21;
  person['city'] = 'Dhaka';

 // these are map properties
  // print(person.length);
  // print(person.isNotEmpty);
  // print(person.isEmpty);
  // print(person.values);
  // print(person.keys);
}